package wire.player;

message Location {
  required float x = 1;
  required float y = 2;
  required float z = 3;
  required float facing = 4;    // degrees, 0=+X
  required float head_tilt = 5; // degrees, 0=horizontal, +=up, -=down
};
